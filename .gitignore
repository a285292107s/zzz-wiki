node_modules
dist
.DS_Store
*.local
.env
.env.*
!.env.example

# ephemeral research artifacts (kept on disk, not in repo)
_research_*
shots

# build-data.mjs 下载缓存（Dimbreath ZenlessData 原始表）
.cache/
# preview 冒烟日志
preview.log

# 项目临时文件（统一放 temp/，不入库）
temp/
